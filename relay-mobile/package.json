{
  "name": "relay-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Relay iOS app — Capacitor wrapper",
  "scripts": {
    "build": "cd ../relay-web && npm run build:mobile && cd ../relay-mobile && npx cap sync ios",
    "open": "npx cap open ios",
    "sync": "npx cap sync ios",
    "copy": "npx cap copy ios"
  },
  "dependencies": {
    "@capacitor/core": "7.6.5",
    "@capacitor/filesystem": "7.1.8",
    "@capacitor/share": "7.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.5",
    "@capacitor/ios": "7.6.5"
  }
}
